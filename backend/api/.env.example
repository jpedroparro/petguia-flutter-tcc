PORT=8090

# Credencial de service account do Firebase (Admin SDK) — baixe em
# Firebase Console > Configurações do projeto > Contas de serviço >
# Gerar nova chave privada. NUNCA commite o arquivo real; só este exemplo.
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
